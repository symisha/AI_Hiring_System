{
  "name": "ai_hiring_system",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "predev:backend": "fuser -k 8000/tcp 2>/dev/null || true",
    "dev:backend": "python3 -m uvicorn app.main:app --app-dir backend",
    "predev:frontend": "fuser -k 8080/tcp 2>/dev/null || true",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hookform/resolvers": "^5.2.2",
    "@playwright/test": "^1.58.2",
    "react-hook-form": "^7.72.1",
    "yup": "^1.7.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
