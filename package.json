{
  "name": "ai_hiring_system",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "dev:backend": "cmd /c \"cd backend && venv\\Scripts\\python -m uvicorn app.main:app --reload\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently --restart-tries -1 --restart-after 2000 -n backend,frontend -c green,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.58.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
